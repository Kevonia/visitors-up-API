# Core FastAPI & ASGI
fastapi==0.115.6          # bumped with Python 3.12; pulls a Starlette new enough for multipart 0.0.30
uvicorn==0.34.0
gunicorn==23.0.0          # prod process manager (multi worker, via UvicornWorker); >=22 (CVE)
python-multipart==0.0.30  # security: clears the parser-DoS HIGHs (needs the newer Starlette above)

# Database & ORM
sqlalchemy==2.0.25
psycopg2-binary==2.9.9
alembic==1.13.1

# Pydantic
pydantic==2.6.1
pydantic-settings==2.2.1

# Authentication & Security
python-jose[cryptography]==3.4.0   # CRITICAL: <3.4.0 JWT algorithm-confusion / DoS CVEs
passlib==1.7.4
bcrypt==4.1.2
cryptography==48.0.1               # security: clears all cryptography CVEs (needs Python 3.12 base above)

# Redis & Caching
redis==5.0.1
rq==1.16.2                 # background worker queue on the existing Redis
hiredis==3.1.0            # Python 3.12 wheels (2.2.3 had none -> tried to gcc-compile)
fastapi-cache2==0.2.1
aiocache==0.12.1

# HTTP Clients
requests==2.33.0          # security: clears all requests CVEs
httpx==0.28.1

# Firebase Cloud Messaging (FCM HTTP v1) — mint OAuth2 tokens from the
# service-account key; messages are sent with `requests` above.
google-auth==2.28.0

# Environment
python-dotenv==1.2.2      # security: clears the dotenv CVE

# Monitoring & Logging
loguru==0.7.2
structlog==23.2.0
sentry-sdk==2.20.0        # security + current FastAPI integration
prometheus-fastapi-instrumentator==7.0.0   # supports the newer Starlette

# Data Handling
pandas==2.1.4

# Testing (Development)
pytest==8.3.5             # patched 8.x; pytest 9 conflicts with pytest-asyncio (dev-only CVE)
pytest-asyncio==0.23.8