# Core FastAPI & ASGI
fastapi==0.109.1
uvicorn==0.27.0
gunicorn==23.0.0          # prod process manager (multi worker, via UvicornWorker); >=22 (CVE)
python-multipart==0.0.18  # security: >=0.0.18 (DoS CVEs); kept FastAPI 0.109-compatible

# Database & ORM
sqlalchemy==2.0.25
psycopg2-binary==2.9.9
alembic==1.13.1

# Pydantic
pydantic==2.6.1
pydantic-settings==2.2.1

# Authentication & Security
python-jose[cryptography]==3.4.0   # CRITICAL: <3.4.0 JWT algorithm-confusion / DoS CVEs
passlib==1.7.4
bcrypt==4.1.2
cryptography==44.0.1               # security: patched (>=44.0.1), keeps py3.9 support

# Redis & Caching
redis==5.0.1
rq==1.16.2                 # background worker queue on the existing Redis
hiredis==2.2.3
fastapi-cache2==0.2.1
aiocache==0.12.1

# HTTP Clients
requests==2.32.4          # security: >=2.32.4 (.netrc/redirect CVEs)
httpx==0.26.0

# Firebase Cloud Messaging (FCM HTTP v1) — mint OAuth2 tokens from the
# service-account key; messages are sent with `requests` above.
google-auth==2.28.0

# Environment
python-dotenv==1.0.1      # security patched line

# Monitoring & Logging
loguru==0.7.2
structlog==23.2.0
sentry-sdk==1.45.1        # security: >=1.45.1 (env-var leak CVE)
prometheus-fastapi-instrumentator==5.9.1

# Data Handling
pandas==2.1.4

# Testing (Development)
pytest==8.3.5             # security: patched 8.x line (avoids the major-9 break with pytest-asyncio)
pytest-asyncio==0.23.8